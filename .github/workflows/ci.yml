name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@v8.0.0
        with:
          python-version: "3.13"
      - run: uv sync --dev
      - run: make lint

  commit-check:
    name: Commit Messages
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v8.0.0
        with:
          python-version: "3.13"
      - run: uv sync --dev
      - run: uv run cz check --rev-range origin/main..HEAD

  unit-test:
    name: Unit Tests (Python ${{ matrix.python-version }})
    # macOS, because the sim extra depends on pyobjc, which has no Linux wheels.
    runs-on: macos-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.13"]
      fail-fast: false
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@v8.0.0
        with:
          python-version: ${{ matrix.python-version }}
      - run: uv sync --dev
      - run: make test
